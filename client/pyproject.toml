[project]
name = "blue-clicker-client"
version = "1.0.0"
dependencies = [
    "textual==8.2.5",
]
requires-python = ">= 3.14.6"

[project.optional-dependencies]
dev = [
    "textual-dev==1.8.0",
]

[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["driver", "manager", "ui", "utility"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP"]
ignore = []